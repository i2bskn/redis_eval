require 'spec_helper'

describe RedisEval do
  it 'has a version number' do
    expect(RedisEval::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
